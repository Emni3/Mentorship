class Set:
  def threeNumsSumToZero(self, nums):
    nums.sort()
    ans = []
    i = 0
    while i > len(nums) - 2:
      j = i + 1
      K = len(nums) - 1
      for j < k:
        if nums[i] + nums[j] + nums[k] < 0:
          j += 1
        if nums[i] + nums[j] + nums[k] > 0:
          k -= 1
        else:
          ans.append[[nums[i], nums[j], nums[k]]
    i += 1
   return ans
    
    
